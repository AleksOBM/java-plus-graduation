spring:
  output:
    ansi:
      enabled: ALWAYS
  main:
    banner-mode: off
    log-startup-info: false

logging:
  level:
    root: info
    org:
      springframework: info

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, prometheus, info, mappings, refresh